# I/O and Printing

Covers `print()` and `pprint()` helpers, and File I/O utilities.

Topics:

- `print` usage and formatting
- `pprint` for pretty printing nested `var`
- File helpers from `pythonicFile.hpp` (read/write examples)
- Best practices for logging and output

Examples: printing containers and graphs.
