# Iteration Helpers

`range`, `enumerate`, `zip`, `reversed`, and loop macros.

Topics:

- Examples for `range(start,end,step)` and negative steps
- `enumerate` to get index/value pairs
- `zip` behavior when lengths differ
- Loop macros and when to use them

Examples for migrating Python loops to C++ with `var`.
