# Strings

Covers string behavior in `var`:

- Storage model (heap-allocated `std::string` inside `var`)
- `str()` vs `toString()` semantics
- Concatenation and operator overloads
- Repetition: `s * n` behavior and examples
  - Document actual behavior for negative/zero/non-integer multipliers
- Slicing on strings
- Conversion helpers (`Str()` / `toString()` usage)

Examples and common pitfalls.
