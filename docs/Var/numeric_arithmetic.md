[⬅ Back to Table of Contents](var.md)

# Numeric / Arithmetic

Numeric and arithmetic operations for `var`.

- Arithmetic operators: `operator+`, `operator-`, `operator*`, `operator/`, `operator%` and compound `operator+=`, `operator-=`, `operator*=`, `operator/=`, `operator%=`
- Numeric helpers: `abs`, `min`, `max`, `sum` (overloads available)
