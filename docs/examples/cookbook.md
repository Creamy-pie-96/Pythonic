# Cookbook

Practical recipes:

- Parse CSV into `list<dict>` using `var`
- Build and run a shortest-path example using `graph`
- High-performance loop pattern using `is_*()` + `as_*_unchecked()`
- Combining comprehensions and functional helpers

Each recipe has code and short explanation.
